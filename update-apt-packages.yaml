---

#- hosts: all
- hosts: all
  become: yes
  tasks:
    - name: Update apt cache
      apt:
        update_cache: yes
       
    - name: Upgrade all packages
      apt:
        upgrade: dist
  #become: true
  #tasks:
  #  - name: Update and upgrade apt packages
  #    apt:
  #      upgrade: yes
  #      update_cache: yes
